# Pi deployment dependencies (LillyCam standard + pro)
# Install into a venv created with --system-site-packages:
#   python3 -m venv --system-site-packages .venv
#   .venv/bin/pip install -r requirements.txt
#
# picamera2 is not listed here: install it from apt (it also provides libcamera
# and a matching numpy); the venv picks it up via --system-site-packages:
#   sudo apt install -y python3-picamera2
# See docs/pi-setup.md.

flask>=3.0
gpiozero>=2.0
luma.oled>=3.13
luma.core>=2.4
smbus2>=0.4
sounddevice>=0.4
numpy>=1.26
python-dotenv>=1.0

# GPIO stack. rpi-lgpio is a drop-in RPi.GPIO shim on lgpio (stepper);
# rpi-hardware-pwm drives the servo via the kernel's hardware PWM. Both work on
# every Pi (incl. Pi 5) and replace the old RPi.GPIO + pigpio/pigpiod stack.
# The servo also needs 'dtoverlay=pwm,pin=12,func=4' in config.txt.
rpi-lgpio
rpi-hardware-pwm>=0.2

# PWA Web Push (optional; only needed when PUSH_ENABLED=true)
# Pulls in py-vapid + cryptography + http-ece. Install on the Pi with:
#   pip install pywebpush
# pywebpush>=1.14

# Dev/test (optional, can skip on Pi)
# pytest>=7.0
# pytest-mock>=3.0
